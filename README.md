# Area-Calculator-Python
A area calculator in Python. It imports the math module and can solve the area of a circle, triangle, rectangle and a square. It is an infinite loop that asks the user for the desired shape by giving a list of numbers with a name. The user then selects the shape and inputs the values that will be calculated and receives the answer.
